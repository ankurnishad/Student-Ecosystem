import { useCallback, useEffect, useState } from 'react'
import { ActivityIndicator, Button, FlatList, StyleSheet, Text, View } from 'react-native'
import { supabase } from '../lib/supabase'
import GroupThreadScreen from './GroupThreadScreen'
type Group={id:string;name:string;description:string|null}
export default function GroupsScreen(){const[groups,setGroups]=useState<Group[]>([]),[loading,setLoading]=useState(true),[selected,setSelected]=useState<Group|null>(null);const load=useCallback(async()=>{setLoading(true);const{data:auth}=await supabase.auth.getUser();if(!auth.user)return;const{data:memberships}=await supabase.from('group_members').select('group_id').eq('user_id',auth.user.id);const ids=(memberships??[]).map(m=>m.group_id);if(!ids.length){setGroups([]);setLoading(false);return}const{data}=await supabase.from('groups').select('id,name,description').in('id',ids).order('created_at',{ascending:false});setGroups(data??[]);setLoading(false)},[]);useEffect(()=>{void load()},[load]);if(selected)return <GroupThreadScreen groupId={selected.id} title={selected.name} onBack={()=>setSelected(null)}/>;return <View style={styles.container}><Text style={styles.title}>My Groups</Text><Text style={styles.sub}>Groups you are actually a member of</Text>{loading?<ActivityIndicator/>:<FlatList data={groups} keyExtractor={i=>i.id} refreshing={loading} onRefresh={()=>void load()} ListEmptyComponent={<Text style={styles.empty}>Abhi aap kisi group ke member nahi ho.</Text>} renderItem={({item})=><View style={styles.row}><Text style={styles.name}>{item.name}</Text><Text>{item.description??'No description'}</Text><Button title="Open" onPress={()=>setSelected(item)}/></View>}/>}</View>}
const styles=StyleSheet.create({container:{flex:1,padding:20},title:{fontSize:28,fontWeight:'700'},sub:{opacity:.65,marginBottom:16},row:{borderWidth:1,borderColor:'#ddd',borderRadius:14,padding:14,marginBottom:10},name:{fontSize:18,fontWeight:'600'},empty:{textAlign:'center',marginTop:30,opacity:.6}})
